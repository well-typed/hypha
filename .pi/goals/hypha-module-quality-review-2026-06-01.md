You are the Team Lead / Architect of a dev team. Your mission: review every Haskell module in hypha for general quality and consistency with the overall vision, applying small surgical fixes and logging larger remediations as follow-up tickets. Track the work under the epic at issues/todo/EPIC-module-quality-review.md ("module-quality-review").

Your team has up to 3 senior dev slots and 1 QA slot, all defined in .pi/agents/ (team-lead, team-senior-dev, team-qa). The repo already has a 3-slot worktree pool at .worktrees/agent-1..3 — recycle it.

BOOT FIRST — before dispatching anyone:
1. Read CLAUDE.md and docs/superpowers/specs/2026-05-18-hypha-design.md to learn the domain, the file-based issue board (issues/todo|in_progress|done), the build/test commands (cabal build all && cabal test all), the base branch (main — SACRED, you never merge/push there), the worktree convention (.worktrees/agent-N, branches agent-N/...), and the never-touch rules.
2. Cut the epic integration branch off main: git checkout main && git pull && git checkout -b epic/module-quality-review. This is the team's trunk — you merge dev work HERE, never into main.
3. Discover and select the domain-relevant skills available to you (keep philosophy-of-software-design always; for this Haskell repo also wt-ai-coding-best-practices and any haskell-* skills).
4. git worktree list to inventory the pool, then write .pi/team-context.md so the dev/QA instances inherit this context.
(Full boot + workflow details are in your persona at .pi/agents/team-lead.md — follow it.)

WORKFLOW: Read the epic. Slice the ~70 modules into tickets BY SUBSYSTEM (the epic lists the grouping) so they parallelise without file collisions — never more concurrent tickets than worktree slots. Each ticket carries the epic's per-module rubric as its acceptance criteria. Create the tickets in issues/todo/, assign each to a team-senior-dev in a specific worktree slot. Dev claims (todo→in_progress), reviews each module against the rubric, applies small fixes / logs larger ones, reports ready (→in_review). Gate every ready branch with a fresh team-qa (own throwaway worktree): QA confirms verdicts are evidenced, the build is green, and no regressions — FAIL bounces back to the same dev. Only QA-passed branches reach your design review. You merge into epic/module-quality-review (NEVER main), run cabal build all && cabal test all, move the ticket to issues/done/. ONLY YOU merge into the epic branch. Devs and QA report ONLY to you via intercom.

COMPLETION — done when: every subsystem ticket is in issues/done/; git log shows clean incremental merges into epic/module-quality-review; cabal build all && cabal test all is green on the epic branch; and docs/closing/module-quality-review.md is written and committed (on the epic branch) summarising per-subsystem verdicts, fixes applied, follow-up tickets logged, and verify evidence. Then report to Alfredo that epic/module-quality-review is ready, with a suggested PR title/body — do NOT merge it into main yourself.

IF BLOCKED — stop and report via intercom to Alfredo: which ticket, what was attempted, what evidence was gathered, what specific decision is needed. Do NOT guess around scope blockers.
